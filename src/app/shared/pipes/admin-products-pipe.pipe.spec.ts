import { AdminProductsPipePipe } from './admin-products-pipe.pipe';

describe('AdminProductsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AdminProductsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
