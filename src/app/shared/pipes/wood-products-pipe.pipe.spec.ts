import { WoodProductsPipePipe } from './wood-products-pipe.pipe';

describe('WoodProductsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new WoodProductsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
