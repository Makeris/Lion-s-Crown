import { AdminOrdersPipePipe } from './admin-orders-pipe.pipe';

describe('AdminOrdersPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AdminOrdersPipePipe();
    expect(pipe).toBeTruthy();
  });
});
